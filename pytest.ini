[pytest]
# `--ds` wins over the DJANGO_SETTINGS_MODULE the compose service exports, so
# the suite always runs on the test settings (dev settings + `tests.testapp`).
addopts = --ds=config.settings.test
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = test_*.py
